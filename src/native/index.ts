/**
 * Placeholder for custom native modules — e.g. a fast YUV→RGB frame
 * pre-processor or a TFLite delegate selector — if the JS/worklet path proves
 * too slow for the sub-1s budget. Empty by design in Phase 1.
 */
export {};
